
spring:
    #Тестовый источник данных
    datasource:
        url: jdbc:h2:file:./h2/lottery;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;TRACE_LEVEL_SYSTEM_OUT=2;TRACE_LEVEL_FILE=0;AUTO_SERVER=TRUE
        username: sa
        password:
    #Отключаем flyway для тестов
    flyway:
        enabled: false
    h2:
        console:
            enabled: true
